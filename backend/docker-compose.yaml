version: "3.9"

services:
  backend:
    build:
      context: .       
      dockerfile: Dockerfile
    container_name: backend-tech2work
    ports:
      - "8000:8000"    # map host:container ports (change if needed)
    restart: unless-stopped
